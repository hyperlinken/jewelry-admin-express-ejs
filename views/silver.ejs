<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>MAGADH — Silver Collection</title>

  <!-- External CSS (same as HTML) -->
  <link rel="stylesheet" href="/csss/style.css"/> 
  <link rel="stylesheet" href="/csss/slideshow.css">
  <link rel="stylesheet" href="/csss/sectionProduct.css">
  <link rel="stylesheet" href="/csss/contact.css">
  <link rel="stylesheet" href="/csss/branchsection.css">
  <link rel="stylesheet" href="/csss/aboutsection.css">

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;700&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">

  <!-- Silver page specific style -->
  <link rel="stylesheet" href="/css/silver.css">

</head>
<body>

  <!-- HEADER -->
  <header class="navbar">
    <div class="navbar-top">
      <div class="logo">
        <a class="logo" href="/">MAGADH</a>
      </div>
      <span class="menu-toggle" id="menu-toggle">&#9776;</span>
    </div>
    <nav class="nav-links" id="nav-links">
      <a href="/collection" data-target="toproduct">Collection</a>
      <a href="/branch" data-target="tobranch">Branch</a>
      <a href="/about" data-target="toinfo">About Us</a>
      <a href="/contact" data-target="Information">Contact</a>
      <a href="/gold">Gold</a>
      <a href="/silver">Silver</a>
      <a href="/diamond">Diamond</a>
      <a href="/gemstone">GemsStone</a>
    </nav>
  </header>

  <!-- HERO -->
  <section class="silver-hero">
    <h1>Silver Collection</h1>
    <p>Elegant silver jewelry with timeless appeal</p>
  </section>

  <!-- MAIN CONTENT -->
  <main class="silver-wrap">
    <!-- Sidebar -->
    <aside class="silver-sidebar">
      <h3>Browse Categories</h3>
      <ul class="silver-types" id="silverTypes">
        <% 
          const categoryOrder = ['Ring','Bracelet','Necklace','Earring','Pendant','Bangle','Anklet'];
          categoryOrder.forEach(category => { 
            const productCount = products[category] ? products[category].length : 0;
            if (productCount > 0) {
        %>
          <li>
            <a href="#<%= category.toLowerCase().replace(' ', '-') %>" data-target="<%= category.toLowerCase().replace(' ', '-') %>">
              <%= category %>s 
              <span class="silver-chip"><%= productCount %></span>
            </a>
          </li>
        <% }}); %>
      </ul>
    </aside>

    <!-- Product sections -->
    <section class="silver-content" id="silverContent">
      <% 
        let hasProducts = false;
        categoryOrder.forEach(category => { 
          if (products[category] && products[category].length > 0) { 
            hasProducts = true;
      %>
        <section class="silver-section" id="<%= category.toLowerCase().replace(' ', '-') %>">
          <header>
            <h2><%= category %>s</h2>
            <div class="silver-count"><%= products[category].length %> designs</div>
          </header>
          <div class="silver-grid">
            <% products[category].forEach(product => { %>
              <article class="silver-card">
                <img src="<%= product.image %>" alt="<%= product.name %>">
                <div class="silver-meta">
                  <span class="silver-name"><%= product.name %></span>
                  <span class="silver-tag">₹<%= product.price.toLocaleString() %></span>
                </div>
              </article>
            <% }); %>
          </div>
        </section>
      <% }}); %>
      <% if (!hasProducts) { %>
        <div class="silver-section"><p>No silver products available.</p></div>
      <% } %>
    </section>
  </main>

  <!-- Back to top -->
  <button class="silver-top" id="backToTop" aria-label="Back to top">↑</button>

  <!-- FOOTER -->
  <div class="footer-bottom">
    <p>© 2025 Magadh Jewels. All rights reserved.</p>
  </div>

  <!-- JS -->
  <script src="/js/silver.js"></script>
</body>
</html>